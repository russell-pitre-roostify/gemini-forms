const now = () => Date.now();

now.id = 'now';
now.label = 'now()';
now.description = 'returns the current date and time value Date.now()';
now.tags = ['convert', 'date'];
now.type = 'Identifier';

export default now;
